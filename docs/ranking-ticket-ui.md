# 랭킹·게임권 화면 보완 및 전체 참가자 테스트 모드

2026-09-26 사용자 요청. 작업 브랜치: `codex/ranking-ticket-ui`.

## 화면 변경

- 중앙 상단 로고는 홈 링크로 동작한다. 기존 라우터를 사용하며 새 탭 열기도 지원한다.
- 게임권이 없는 일반 상태에서는 시작 버튼이 `친구에게 공유하고 게임권 받기`로 바뀌고 초대 화면으로 이동한다. 행사 중단·종료 및 진행 중 게임 복원 규칙을 유지한다.
- 랭킹 상단을 TOP3 선물(1위 5만원·2위 3만원·3위 1만원)로 교체했다.
- 내 순위·최고 점수·현재 버전 랭킹에 기록이 있는 참가자 수를 표시한다.
- 시간 차이는 최고점 세션의 실제 `valid_ticks / 60`으로 계산한다. 점수는 코인 보너스를 포함하므로 시간순과 점수순은 다를 수 있다. 동점 3위는 기록 달성 시각, 참가자 ID 순으로 정한 대표 기록과 비교한다.
- 랭킹 화면에서 이전 규칙 수령 요청 및 합성 테스트 입력 카드를 제거했다. 서버에 저장된 접수 기록은 보존한다.

## 테스트 기간의 무제한 플레이

- 사용자가 특정 브라우저 방식 취소 후 **모든 참가자** 적용을 요청했다.
- `PREVIEW_UNLIMITED_PLAY=true`이면 local/test/preview의 활성 합성 참가자 모두 게임권 차감 없이 플레이한다. 기본값은 false다.
- 참가자 ID 허용 목록은 더 이상 사용하지 않는다. Vercel의 이전 참가자 ID 설정도 제거한다.
- 무료 게임 장애 시 환급을 새로 발행하지 않는다. 복주머니 1회, 차단·행사 상태·세션 검증은 유지한다.
- 테스트를 마치면 환경변수를 false로 바꾸고 재배포해 일반 게임권 방식으로 복귀한다.

## 검증

- Node 154개 통과, Python 182개 통과(생략 없음).
- 여러 참가자의 무료 플레이, 요청 본문 조작 거부, 무료 세션 무환급, 테스트 모드 해제, 실제 시간·동점·게임 버전 격리를 검증했다.
- 로컬 브라우저에서 TOP3 선물·4위/450점/1,234명·12.3초 차이 예시와 로고 홈 이동을 확인했다. 예시 데이터는 `.local` 검토 서버 전용이며 배포 데이터에 추가하지 않는다.
- 원격 부하 테스트는 실행하지 않는다.

## Preview 반영 결과

- 제품 커밋 `fb5affd078c121532c4439ee792a86fb03965bad`, Vercel `dpl_2ZT6tjjp8SEqeYgvuV9CtTdLPSFY` (`READY`).
- https://google-korea-team-gemini.vercel.app/ 에 연결했다. DB 준비 상태와 배포된 홈·랭킹·CSS 파일 일치를 확인했다.
- 새 합성 참가자 2명으로 각각 접속→게임 예약→잔액 조회를 수행했다. 두 참가자 모두 무제한 true, 무료 세션 NOT_DUE, 잔액 미차감이었다. 총 8회 API 요청이며 게임 실행·추첨·부하 테스트는 하지 않았다.
- 실제 브라우저에서 전체 무제한 안내와 새 랭킹 화면을 확인했다.
- GitHub PR #7에 보관하며, main 병합은 하지 않았다.

## 튜토리얼 진입 수정

- 홈의 별도 `조작 방법과 규칙` 버튼을 제거하고, 새 게임 시작을 누를 때마다 튜토리얼을 표시한다. 이전 열람 여부로 생략하지 않는다.
- 튜토리얼 완료 또는 `건너뛰고 게임 시작`으로 게임에 진입한다. 진행 중 게임 복원과 게임권 부족 시 공유 동선은 유지한다.
- Node 154개 통과. 제품 커밋 `c1a84e4`, Preview `dpl_TVkmr6s6q72J5HKLPeE89cPpAwSn`을 같은 테스트 주소에 반영했다.

## 게임 영역·랭킹 목표 HUD

- 게임 박스를 16:15 비율로 세로 확장했다. 렌더링 전용 상하 여백을 추가하며 960×600 물리 좌표·충돌·점프·점수 규칙은 유지한다.
- 게임 박스 바로 아래에 로고와 `Built with Google Antigravity`를 표시한다.
- 게임 시작 때 현재 규칙의 TOP3 점수 기준을 한 번 조회한다. 현재 판의 점수 옆에 가장 가까운 상위 목표까지 남은 점수를 표시한다. 동점 점수 도달도 인정해 🥉→🥈→🥇으로 전환하며, 부활 감점 이후에는 다시 계산한다. 1위 점수 도달 시 `🥇 목표 달성!`을 표시한다.
- 서버의 dense rank와 동일하게 다른 점수 세 개를 기준으로 한다. 비공개 참가자는 이름을 노출하지 않고 점수 기준에 포함한다. 기록이 없으면 첫 기록 안내, 기준이 1~2개면 존재하는 순위만 목표로 표시한다.
- 랭킹 요청 실패·다른 버전·늦게 도착한 이전 게임 응답은 현재 게임에 영향을 주지 않는다. 게임 중에는 랭킹을 반복 요청하지 않는다. 최종 순위는 게임 종료 후 서버 검증 시점에 확정한다.

- 조작 안내는 터치 아이콘과 함께 좌우 두 칸으로 표시한다: `탭 → 점프`, `꾹 누르기 → 수퍼 점프`.
- 검증: Node 전체 174개, 이후 최종 UI/복원 검사 17개, 서버 Phase2 12개 통과. 320px/440px 화면에서 HUD 배치와 목표 전환을 확인했다. 로컬 Python 캐시 읽기 지연은 별도 임시 환경·캐시로 우회해 검증을 완료했다.
